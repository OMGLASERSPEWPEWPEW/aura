# Aura Environment Variables

# =============================================================================
# API CONFIGURATION
# =============================================================================

# Option 1: Production Mode (Recommended for deployed apps)
# Uses Supabase Edge Function proxy - API key stays server-side
#
# VITE_USE_PROXY=true
# VITE_SUPABASE_URL=https://your-project-id.supabase.co

# Option 2: Development Mode (Local development only)
# Direct API calls - API key exposed in browser bundle
# WARNING: Never deploy with this mode - your key can be stolen!
#
# VITE_USE_PROXY=false
# VITE_ANTHROPIC_API_KEY=your_anthropic_api_key_here

# =============================================================================
# CURRENT CONFIGURATION (modify based on your needs)
# =============================================================================

# For local development (direct API access):
VITE_USE_PROXY=false
VITE_ANTHROPIC_API_KEY=your_anthropic_api_key_here

# For production (proxy mode):
# VITE_USE_PROXY=true
# VITE_SUPABASE_URL=https://your-project-id.supabase.co
